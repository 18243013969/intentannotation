apply plugin: 'java-library'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation dps.annotation
}

sourceCompatibility = dps.sourceCompatibility
targetCompatibility = dps.sourceCompatibility